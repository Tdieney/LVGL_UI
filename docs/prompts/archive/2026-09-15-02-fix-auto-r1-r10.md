> ARCHIVED — historical context only. Superseded by [CURRENT.md](../CURRENT.md).
> Preserved 2026-09-15 before the layout/review follow-up handoff. Original body follows unchanged.

# CURRENT — fix all Auto v1 independent-review findings R1–R10

Date: 2026-09-15. Status: **READY FOR HANDOFF — fixes not yet verified**.

Owner request: fix ALL independently reviewed defects in one implementation pass.
Keep Auto with LCD-editable thresholds. This is the sole active handoff, replacing
the previous implementation prompt. The requirements retain the technical-lead
specification; do not substitute the implementation author's altered defaults or
completion claims. The current authoring turn changes documentation only.

---

You are the implementation engineer in
`C:\Users\vanth\Documents\MyDocument\Embedded\LVGL_UI`.

Implement, test and document the scoped corrections below in the existing code.
Do not stop at a plan or request routine confirmation to begin the authorized fix
workflow. Ask only if a genuinely missing decision blocks safe progress. No
unrelated work, redesign, commit or push is authorized.

## 1. Read first and preserve existing work

Read `AGENTS.md`, `docs/prompts/README.md`, this entire file,
`docs/reviews/2026-09-14-auto-v1-independent/REVIEW.md` and its diagnostic sources,
`docs/README.md`, `docs/HARDWARE_OVERVIEW.md`, `docs/MCU_BASELINE.md`,
`docs/UI_DESIGN_BRIEF.md`, `docs/user/SMART_HUB_UI_GUIDE.md`, `LORA_PROTOCOL.md`,
and current implementation/tests. Inspect Git status. Preserve all pre-existing
edits; no reset, clean, stash, branch change, commit or push without a separate request.

The submitted baseline passes build, 24 regression checks and 15 scenarios, but
independent checks found R1–R10. Several tests assert the wrong expectation or bypass
real interaction paths. Neither 24/24 nor the submitted REPORT.md is acceptance.
Archived prompts are historical context, not a second active specification.

## 2. Constraints and verified behavior to preserve

- LVGL 8.4, 800x480 RGB565, one 800x10 partial buffer, current 42 KiB LVGL heap.
  No MCU framebuffer, heap increase, per-tick allocation, float formatting or
  trigonometry to hide defects or make tests pass.
- Keep Study 12 flat light palette, borderless cards, visible fonts >=18 px,
  splash, elapsed-time header, Connected wording, SNR bars and three icon-only
  navigation targets. Keep the 440x368 modal and its internal Device/Auto thresholds
  subviews. Local interaction/spacing fixes only; no new main page, nested modal,
  Details tab, notification panel or broad redesign.
- Hub owns appliance semantics, active_low and three Auto rules. Node applies raw
  GPIO levels verbatim. Preserve direct globals in `lora_comm.h`/`lora_hub_link.h`,
  the 8-byte command and 20-byte status packet. No production snapshot layer,
  remote-settings ACK mechanism or general-purpose scheduler/automation framework.
- No radio, Modbus, Flash/NVS drivers, Node watchdog, multi-node, scenes or schedules.
  Persistence remains a config-change callback plus correct integration examples.
  Control and LVGL run in application context, never ISR.
- Preserve independently verified fixes: pre-init config staging, idle raw-GPIO
  reconciliation across all 16 masks, Trends Y-axis invalidation/size updates,
  zero redraw on identical frames/refeeds, and removal of false Ideal captions.
  Do not overwrite legitimate pending intent with older reports or another channel.
- Keep 16-sample session-RAM history. Do not invent 24-point/24-hour retention,
  health standards, ppb-to-Sensirion conversion or target validation claims.

## 3. Resolve every finding, keeping these review identifiers

### R1 [P1] Valid explicit opt-in is required for Auto

Observed: boot modes Auto/Auto/Auto/Manual; without user input command mask becomes
7. Invalid threshold config falls back to Auto; schema 255 passes validation;
legacy migration retains Auto although old Auto was only a label without thresholds.

Fix:
- Factory defaults and missing/invalid/legacy threshold records yield Manual.
  Only complete valid current-schema config explicitly enabling Auto on a supported
  preset may restore Auto. Unsupported presets remain Manual-only.
- Validate record size/version before deserialization, schema, bounded name,
  preset, mode, active_low, bounds and threshold ordering. Never read a truncated
  old record as the expanded struct. Share canonical validation/defaults across
  restore, APIs and editor. Document fallback/recovery indication.
- Preserve valid recoverable legacy metadata but supply default threshold pairs +
  Manual if thresholds were absent or invalid. Restoration causes no save callback.
- Manual fallback does NOT force physical outputs OFF: preserve non-actuating
  startup synchronization and adopt fresh GPIO readback before permitting TX.
- Correct CHECK 20's wrong expectation that legacy Auto remains enabled.

Tests: cold boot with no config, valid explicit current Auto, legacy Auto, unknown
schema, malformed pair, truncated record and invalid metadata. Observe TX bytes
for >10 seconds; no default/fallback Auto actuation. Both polarities, all 16 masks.

### R2 [P1] Enforce RX -> reconcile/control -> gated TX

Observed: copyable firmware loop sends periodically before `ui_tick()` without
checking `ui_is_tx_ready()`, defeating the new boot/reconnect protection.

Fix the executable integration example, simulator ordering and send helpers
together: publish RX, reconcile/control, then transmit only if readiness permits.
Gate new commands AND periodic retransmission before first eligible report, during
loss and on reconnect. No zero initialization or stale pre-disconnect intent may
leak on TX. Keep rendering/input in application context. Do not weaken freshness
or make the Fake Node edge-triggered to conceal wrong GPIO packets.

Tests: actual send-order logic with a recording TX stub before init/first report/
first reconciliation, after staleness and at the first reconnect iteration. A
readiness getter queried in an already-running settled UI is insufficient.

### R3 [P1] Unify runtime configuration commits

Observed: public setter enables Auto and issues a command in 300 ms, then accepts
active_low changes while pending; dialog Save follows different restrictions.

Fix:
- Distinguish boot restore from runtime commit. Route allowed runtime API edits
  and dialog Save through ONE validator, hold/reset policy, pending guards and
  callback contract. If an API is boot-only, enforce/reject runtime use clearly
  and migrate all callers; no unrestricted assignment path may remain.
- Enabling Auto/changing its committed limits restarts qualification and 10-second
  hold without immediate output changes. No-op Save does not reset state or invoke
  persistence again. Reject invalid runtime edits without replacing active config
  with defaults. Return/document observable rejection results.
- Refuse unsafe preset/polarity changes while pending with no partial config/wire
  mutation. A permitted live polarity edit preserves logical intent and tracks
  any real GPIO command through the shared arbitration path.
- One committed local change invokes the callback once; restore, draft edits,
  cancellation, rejected edits and no-op Save invoke it zero times. Config needs
  no remote-settings ACK. One channel must not overwrite another's pending intent.

Tests: API/dialog parity, enable/limit-edit hold, no-op Save, invalid runtime edits,
pending polarity/preset refusal, callbacks, channel independence and offline edits
followed by reconnection. Test real entry points, not privileged field injection.

### R4 [P1] Reconnect must not clear a latched Auto timeout

Observed: paused_error=1 before loss becomes 0 on reconnect without explicit Retry.

Separate initial creation, recovery resynchronization and explicit re-arm.
Reconnect adopts fresh GPIOs, abandons stale in-flight tracking and restarts hold,
but preserves a latched command error. Late matching reports, sensor recovery,
changed limits, tab/modal changes and ordinary Save must not clear that latch.

Only explicit Retry or a deliberately committed Manual -> Auto transition re-arms.
Auto Retry adopts CURRENT fresh report/config, resets hold/qualification and never
blindly resends an obsolete target. Manual Retry retains manual intent. Keep a
usable explicit re-arm route when late readback already settled the old command.
Represent possible late packets/transport retries honestly; actual output can
update without resuming automatic decisions.

Tests: timeout -> loss -> reconnect, timeout -> late confirmation, limit edits
after timeout, invalid-input Retry and explicit re-arm. Leave fresh high readings
for >10 seconds without re-arm and prove no NEW automatic command sequence.

### R5 [P2] Sensor suspension and recovery need explicit state

Observed: invalid sensor still displays Auto; three recovered 1-second frames can
command an output without restarting the required 10-second hold.

Track transient sensor/link suspension separately from latched command errors.
Show Auto paused when decisions are suspended, retaining actual fresh relay
readback where available. No NEW Auto decisions on invalid/out-of-wire-range
inputs or relevant fault bits. Unrelated sensor failures must not pause healthy
rules; input validity uses wire ranges, not editor bounds.

On sensor recovery, restart hold/qualification ONCE, not every subsequent frame.
Use newly received eligible revisions only; reset interrupted qualification and
deadband counters, with wrap-safe timestamps. Keep last commanded level on sensor
failure without claiming it is safe or OFF; previously issued commands remain tracked.

Tests: long healthy interval -> fault -> recovery, repeated invalid frames, noisy
boundaries, duplicates, unrelated faults, each rule, every tab/modal/splash. No
transition before restarted hold plus fresh qualification; no new latched error
for a merely temporary sensor fault.

### R6 [P2] X must cancel via real pointer hit testing

Observed: a later full-size clickable subview intercepts taps on the visible X.

Fix stacking/hit testing in BOTH Device and Auto thresholds views while preserving
working child controls. X closes/discards even invalid drafts; Back retains draft
without saving. The user must not be forced to Save to exit. Keep required >=44x44
touch targets nonoverlapping, >=18 px text and existing modal bounds. Block the page
under the modal without blocking the modal's own controls.

Tests: real synthetic pointer press/release at X coordinates from each view,
including repeated Back/Set limits, invalid drafts and incoming telemetry. Assert
dismissal, unchanged committed config/wire intent, zero callbacks and no click-through.
Calling a close callback directly does NOT satisfy this test.

### R7 [P2] Preserve one draft, including polarity, across subviews

Observed: a real polarity tap changes widget=1 but draft=0; Set limits destroys the
widget, and Save from Auto view commits the old polarity. Helpers currently hide it.

Use real widget events or capture controls before destruction to maintain one
coherent draft: preset, mode, active_low and both thresholds. Save from either
view validates/commits the whole draft once. X discards everything; Reset limits
changes only draft limits. Auto underneath continues with committed config.
Test helpers must not inject state that real events never update.

Tests: real polarity tap -> Set limits -> Save, reverse traversal, combined edits,
invalid pair -> Back -> X, Reset -> Cancel and reopen. Verify bytes, callbacks and
per-device isolation, including pending/offline restrictions from R3. No unsaved
edit may reach the active controller or generate an output command.

### R8 [P2] Restore the specified limits/defaults exactly

Use one canonical table for editor, defaults and validation. These are demo values,
NOT health or appliance protection standards. Do not silently substitute values.

| Preset/input | Default ON | Default OFF | Editable bounds | Step | Required gap |
|---|---|---|---|---|---|
| Ventilation Fan / CO2 ppm | >=1000 | <=800 | 0..10000 | 50 | ON >= OFF + 50 |
| Air Purifier / unitless VOC | >=150 | <=100 | 1..500 | 5 | ON >= OFF + 5 |
| Humidifier / %RH | <=40 | >=50 | 0..100 | 1 | OFF >= ON + 5 |

- Independent values per device, including two with the same preset. Inclusive
  bounds; valid imported integers need not be exact step multiples.
- Steppers clamp individually. Crossing the required gap creates an invalid draft,
  local explanation and disabled Save; do not silently change the other threshold.
- Keep plus/minus, bounded long-press repeat without an extra release step, Reset
  limits, readable values/comparators/unit and Save visible without scrolling.
  No keyboard/new page. Set limits remains usable while Manual.
- Changing draft preset loads its default pair, never cross-unit values. Light,
  Plug, Generic, Heater and Dehumidifier remain Manual-only; no new rules.

Tests: literal expected values independent of production macros, bounds/gaps,
restore/reset, long-press/release, same-type device isolation and actual output
decisions using edited limits instead of default constants.

### R9 [P2] Fix MCU font configuration AND honest production verification

Observed: documented Montserrat-14 disable still fails compilation; smoke uses
simulator config and Release -DNDEBUG removes its asserts. With assertions enabled,
readiness fails because last_rx_tick=1000 while LVGL tick is still zero.

Fix:
- Publish a complete supported LVGL font config, shared consistently by simulator,
  MCU example and docs. Keeping Montserrat 14 as an internal default is acceptable
  while visible UI retains custom >=18 px fonts. Glyph/geometry checks are not a
  replacement for the missing default-font configuration fix.
- Use always-on smoke checks with nonzero failure exits, not plain assert() removed
  by Release. Fix fixture timestamps/freshness/sequence; never weaken production
  checks for a fixture. Demonstrate a deliberately failing check is detected in
  the SAME Release configuration as the successful production smoke.
- Add repeatable clean host production compile/link/run using ONLY staged MCU
  export sources/headers, documented config and minimal application stubs. No
  UI_TEST_HOOKS or test APIs. Compile LVGL for that config in a separate build
  directory: reuse cached dependency SOURCE offline, not a differently configured
  library or hidden repo-only includes. Verify all exported dependencies resolve.
- Integrate this into a documented verification command with failure propagation.
  Keep export --verify explicitly a manifest check, not runtime/target validation.
- Fix contradictory current docs, retired snapshot/overlay APIs and inaccurate
  sample counts/footprints. Report only values actually measured.

Evidence: active Release checks, fail-detection negative control, successful staged
export compile/link/run with documented font config, commands and config/source
provenance. Account for both original failures; the deliberately broken historical
font configuration itself does not need to become supported.

### R10 [P2] Switch positions must show confirmed readback

Observed: pending switch checked=1 while fresh active-high readback GPIO=0.

On Home AND Devices use reported_gpio XOR active_low, even while pending. Keep
desired intent in command state, show Sending separately and reject repeat gestures.
If LVGL toggles a switch before its handler, immediately restore displayed readback;
do not leave an optimistic frame until another packet arrives. Preserve honest
offline/timeout/late-report display.

Accepted switch gestures in Auto atomically commit Manual plus the opposite of
fresh reported output, with no timed return to Auto. Rejected gestures do not
change mode. Save/enable Auto is not an output confirmation.

Tests: real pointer ON/OFF on both pages/polarities, Auto/Manual, before ACK,
after ACK, timeout, retry and late readback. Assert widget state AND pixels against
command/readback bytes, not only captions.

## 4. Cross-cutting behavior that must survive

- Auto evaluates newly received frames on every page, during splash and settings.
  A decision requires 3 consecutive eligible fresh frames plus a 10-second minimum
  hold after confirmed transition, enabling Auto, changed committed limits,
  boot/reconnect synchronization or sensor recovery. A fresh frame must trigger
  the decision; elapsed time alone cannot. Bounded state, wrap-safe tick/revision.
- Do not reverse/supersede pending commands with Auto. Preserve per-channel seq,
  manual retry and late readback. After a pending result, evaluate then-current
  config, not a decision queued under old thresholds. Adopt unsolicited changes
  first and restart hold; do not fight that report on the same frame.
- No-op Save must not restart control. Manual stops NEW decisions, not a packet
  already sent. Explicit re-arm is not the same operation as resynchronization.
- Lost link cannot guarantee physical OFF. No new forced-off policy, fake safe
  state or Node feature is introduced here. Use suitable demo loads; actual MCU,
  radio, touch and load safety remain target-validation work.

## 5. Required workflow and acceptance evidence

1. Reproduce R1–R10 before fixing. Sources/commands are in
   `docs/reviews/2026-09-14-auto-v1-independent/`. Its observation harness can
   exit 0 while printing a defect: interpret output and add maintained assertions.
   If APIs change, document how each reproduction migrates without bypassing the
   original path. Preserve prior evidence; do not edit it to hide failures.
2. Fix P1 R1–R4 first, then R5–R10. Retain useful existing checks and 15 scenarios.
   Change wrong expected behavior only with its contract citation and replacement
   assertion. Do not weaken/delete tests or stop merely because the old suite passes.
3. Require LVGL input-path tests for R6/R7/R10; helper setters/callbacks alone are
   insufficient. Combine error/recovery/edit/override sequences, not only isolated
   rule tests. Assert transmitted bytes and reported outputs as well as UI state.
4. Preserve boot/mask checks, in-place Y-axis pixel correctness, chart metric/
   negative-value behavior, tooltip update/clear on history change and no false
   comfort captions. Ten identical frames/refeeds on Home/Trends/Devices must have
   zero redraw excluding separately measured uptime/animation/real transitions.
   A live Current-only update must not repaint the chart.
5. Run >=50 page/modal/subview cycles and sustained Auto transitions. Report peak
   free heap, largest block, fragmentation and drift within unchanged 42 KiB,
   including dropdown-open/invalid-editor states. Require >4 KiB free at tested
   peaks and no progressive leaks. No full-screen redraw workaround.
6. Run `tools/build_sim.bat`, `tools/run_regression.bat`,
   `tools/export_mcu.bat --verify` and the new production verification command.
   Capture exact commands, exit codes, test names and measurements. Keep cached
   LVGL dependency updates disconnected after the initial download.
7. Generate/inspect native 800x480 LVGL screenshots: both settings views, invalid
   thresholds, sensor pause, error latch after reconnect and reported switches
   before/after confirmation. Include pointer-test evidence for cancel/draft;
   screenshots alone do not prove hit testing. No clipping or overlapping controls.
8. Save new report/logs/screenshots in `docs/reviews/2026-09-15-auto-v1-fixes/`,
   not over the old independent review. Matrix: R1–R10 -> files -> test name ->
   pre-fix failure -> post-fix result -> evidence. Clearly separate manifest,
   host build, runtime checks and untested target hardware.
9. Update PLAN, DEV_LOG, integration docs and relevant current product notes.
   Preserve history but supersede false current completion claims. Do not edit this
   prompt to legalize divergent behavior or create another prompt in a review
   folder. Do not claim independent acceptance of your own implementation.

Complete means all R1–R10 have passing acceptance evidence while verified behavior
is preserved. Otherwise list remaining findings as open. Final response: concise
R1–R10 status, actual tests, report/screenshot links and target-only limitations.
No commit or push.
