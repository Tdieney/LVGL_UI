> ARCHIVED — historical context only. Active handoff: [CURRENT.md](../CURRENT.md).
> Preserved after independent review of the W1–W4 implementation on 2026-09-16.

# CURRENT — close W1–W4 after the V1–V4 implementation review

Date: 2026-09-16. Status: **IMPLEMENTED — awaiting review**.

Owner rule: every review with remaining issues automatically supplies CURRENT.
The independent review accepts the current visual direction; remaining work is
re-arm usability, transient heap headroom, real verification and export ownership.
No broad redesign. This prompt-writing turn changes documentation/diagnostics only.

## 1. Read and implement within scope

Read AGENTS.md, docs/prompts/README.md, this file,
docs/reviews/2026-09-15-auto-v1-v4-independent/REVIEW.md and check.c,
docs/README.md, docs/HARDWARE_OVERVIEW.md, docs/MCU_BASELINE.md,
docs/UI_DESIGN_BRIEF.md, docs/user/SMART_HUB_UI_GUIDE.md,
docs/user/LORA_GUIDE.md, LORA_PROTOCOL.md and current code/tests.
Inspect Git status; preserve all user work and historical review evidence.

Implement/test/document W1–W4, not just a plan. No commit/push, branch changes,
reset/clean/stash, unrelated refactors, RF/Modbus/Flash drivers or new features.
Archived handoffs are historical context, not additional current requirements.

The build and 31 maintained checks currently pass. Independent probes still fail
on missing Retry after recovery and transient free heap below4096. Production
verification successfully builds the actual exported source package and LVGL
separately; retain that improvement.

## 2. Freeze the working visual/product baseline

- LVGL8.4,800x480 RGB565; one800x10 partial buffer; unchanged42KiB heap.
  No MCU framebuffer, per-tick allocation, float formatting or trigonometry.
  Use process-lifetime static captions, fixed value buffers and change guards.
- Keep light/flat Study12 colors, borderless surfaces, existing icons/fonts/shadows,
  splash, uptime, Connected/Disconnected, SNR bars and three icon-only tabs.
  Visible text >=18px. No gradient, global recoloring, new page or notification panel.
- Keep current balanced Temperature/Humidity cards236x98 at(544,68)/(544,178),
  title/value composition, inline units and truthful No data/Offline dash display.
  No false comfort footer or stale numbers.
- Keep modal(180,56,440,368), padding20, shared Save at content(0,280,400,48),
  current Device/Auto rows,44px controls,48px steppers, five-digit value column,
  fixed validation slot and popup font18/44px pitch/border0/bounded viewport.
- Keep the two-row Home composite error layout now implemented. Only add the
  missing recovery-latched action state described in W1; do not redo all cards.
- Hub owns presets/polarity/Auto; Node applies raw relay GPIOs. Preserve direct
  lora_comm.h/lora_hub_link.h globals and8-byte/20-byte packets. No snapshot layer,
  Node automation/watchdog, multi-node, schedules, remote-settings ACK or drivers.
- UI/control runs in application context, not ISR. Preserve16-sample session-RAM
  Trends, chart interactions, readback honesty and zero-redraw/idempotence behavior.

## 3. W1 [P2] Keep an accessible Retry while an Auto error remains latched

Reproduction: Auto command times out -> reconnect OR matching late ACK -> command
phase becomes IDLE, paused_error stays true, Retry disappears on Home AND Devices.
CHECK30 invokes a retry helper directly and therefore misses the unavailable button.
The existing Manual->Auto settings route still works; do not misreport total
inability to recover. Fix the missing explicit re-arm affordance.

Separate transport command phase from Auto error-latch display:
- For an online device in Auto with paused_error=true, retain a visible,
  pointer-accessible Retry/re-arm, even if command phase has settled to IDLE.
- Preserve the current UNKNOWN+Retry presentation for an unresolved command error.
  When fresh readback is known after reconciliation/late ACK, do not falsely call
  the output Unknown solely to make Retry show.
- Technical-lead local layout direction for the new settled-but-latched state:
  full-width Auto paused in the first metadata row; actual readback switch at
  the left of the lower row and Retry at its right. The181x116 Home tile has room
  for two54px visual controls with a clear gap. Expand effective targets to>=44px
  high as needed, without overlap or leaving the tile. Keep text>=18, existing
  palette/title/card bounds. Use equivalent separated controls on Devices.
  This is a targeted state variant, not a change to ordinary settled layout.
- Accepted switch gestures still mean explicit Manual override; Retry means
  re-arm Auto using CURRENT fresh report/config and restart hold/qualification.
  Neither gesture may replay an obsolete desired target.
- Do not clear the latch on reconnect, late ACK, tab changes, ordinary Save or
  threshold edits. Do not show an enabled re-arm while offline or create a fake
  safe/OFF state. Retain current validation for pending/offline/invalid-input actions.
- Retry itself does not directly change output or create a new sequence before
  qualification. Preserve no-op/callback contracts and channel isolation.

Required real-pointer tests, on both pages and both polarities:
1. Reach pending through real Auto decisions, then a real timeout; assert latch.
2. Matching late ACK -> IDLE: assert fresh switch state AND visible Retry.
3. Loss -> reconnect -> IDLE: assert readback adoption, latch and visible Retry.
4. Tap visible Retry by coordinates; assert latch clears, hold restarts and no
   immediate obsolete packet. Qualify new frames only after required hold.
5. Ordinary edits/navigation cannot clear latch; offline cannot re-arm.
6. Test actual switch-based Manual override from the new variant.

Do not satisfy these tests by calling hidden-button callbacks or privileged
ui_trigger_device_retry() alone. Keep the existing underlying retry logic if it
already meets the contract; fix how it is presented and exercised.

## 4. W2 [P2] Measure and fix actual all-state/transient heap headroom

CHECK31 samples memory only AFTER closing settings and returning Home, so its
peak82%/minimum7784 misses expensive views. Independent post-suite samples:
Device settings first rendered checkpoint3896 free; after another40ms4744;
dropdown4408; Auto limits first checkpoint4024. The first/last values violate
the >4096 reserve. These are transient dips, not a proven leak or observed OOM.

Change maintained instrumentation to sample:
- Before/after navigation and page teardown/build at meaningful application-context
  boundaries, including async-delete overlap/first rendered frame.
- Device view open, popup and scrim open, selection/dismiss, Auto subview open,
  invalid pair with visible validation, Back and cancellation.
- Stable checkpoints for drift separately from transient minima.
- At least50 complete cycles with real invalid drafts and recovery/Auto activity.

Record minimum total free, minimum largest block, actual peak usage, fragmentation
and stable used-memory drift, with the state/time each minimum belongs to.
Assert >4096 free throughout tested peaks, not only final Home.
Largest block and total free are different; do not conflate them.
Use a meaningful deterministic drift threshold and explain allocator warm-up;
do not silently loosen thresholds to make the result green.

Fix actual allocation/lifecycle overhead within42KiB. Safe deferred teardown/
creation scheduling or reducing redundant styles/containers is acceptable;
do not delete LVGL event targets unsafely, accumulate live subviews, drop readable
fonts or enlarge heap. Do not hide a low first-frame sample by simply waiting
another tick before measuring. If sequencing is changed to avoid overlap, exercise
rapid pointer/navigation and cancellation to prove no stale pointers, dead taps,
visual flicker or state loss.

Preserve the now-working popup scrim/X/selection lifecycle, static caption/value
ownership and identical-frame zero redraw. Submit new measurements; do not reuse
the review's numbers as measurements of your corrected build.

## 5. W3 [P2] Make tests exercise the claimed production paths

### Recording TX, not just readiness checks

CHECK29 currently inspects readiness/seq but records no sends.
Add a host recording TX stub called by the actual integration-loop code under test:
publish RX -> ui_tick reconciliation/Auto -> application-context LVGL input ->
gated periodic/sequence-triggered TX. Share/compile the documented example where
practical; do not test a disconnected imitation with different ordering.

Record count, tick and full command bytes for each send. Assert zero sends before
first eligible report, while stale and before reconnect reconciliation; assert
correct adopted GPIO mask on the first permitted reconnect send and no stale
intent. Verify new-sequence delivery and unchanged-sequence periodic retransmission
at the intended interval. Include all16 readback masks and both polarities.
An already-settled ui_is_tx_ready() getter is not evidence of send ordering.
No radio driver or production packet change is required.

### Real restore and sensor recovery

Keep the repaired record-kind/length adapter and real API signatures.
Add fresh-process/fixture tests that call the actual adapter BEFORE ui_init, then
ui_init and normal reconciliation, without forcing s_test_boot_staging in a
running UI. Assert preserved config/defaults, no restore callbacks, blocked initial
TX and adoption before sends. Keep the existing helper test as partial coverage,
not the sole boot proof.

Exercise every truncated supported record length, wrong kind/version, malformed
name/preset/mode/polarity/thresholds, missing record and complete valid/legacy
records against real headers. Unknown provenance cannot be guessed from length.
Use the same documented adapter/default policy, not divergent duplicate logic.

CHECK30 does not actually invalidate/recover a sensor. Add a real healthy ->
relevant sensor-invalid/fault -> valid sequence, then prove the full recovery hold
restarts once, duplicate frames/time alone cannot qualify and unrelated faults
do not pause healthy rules. Cover all three rules and keep timeout/late-ACK/loss/
reconnect/explicit re-arm sequences distinct, including W1's pointer route.

### Input tests and honest reports

Create invalid limits via actual plus/minus pointer gestures, not only helper
setters. Register the callback counter BEFORE editing/cancelling; assert zero
callbacks, unchanged committed config/wire/mode and draft discard after both
Auto X and Back->Device X. Test polarity->limits->Save through actual widgets.

For popup drag, assert the viewport really moved and select first/last options
through pointer input, rather than resetting scroll immediately after an unchecked
drag. Preserve verified list geometry/style and no click-through.

Integrate these into tools/run_regression.bat, with assertions active in Release
and process watchdogs. Assertions must fail on deliberately wrong expectations/
test doubles that send when gated or omit periodic retransmission.
Do not weaken the old tests, add impressive names without coverage, or claim
independent acceptance of your own implementation. Preserve historical probes;
the old source-line numbers are historical, not current authority.

## 6. W4 [P2] Verification must not overwrite or delete another export

The verifier calls export_mcu.bat, which names a root ZIP to the minute and uses
Compress-Archive -Force. Filename-diff tracking occurs AFTER possible overwrite.
A pre-existing same-minute ZIP or concurrent invocation defeats ownership detection.

Make verification export to an exact unique task-owned path/directory, or introduce
a collision-safe output option while preserving ordinary export usability.
Never overwrite an existing archive by default. Track the actual returned artifact
path, not root before/after filename differences. Cleanup only exact files owned
by that invocation. Canonicalize/check absolute paths with directory boundaries
before recursive deletion; do not rely on an ambiguous string prefix alone.

Retain actual artifact extraction/build, separate offline LVGL compilation,
production UI without test hooks, stage/config provenance, timeouts and strict
negative control (successful launch, exact exit1 and expected stderr).
Launch/crash/timeout errors must propagate, never count as expected failure.
Keep diagnostics on failure. Tests:
- Pre-existing sentinel ZIP remains byte-identical after verify.
- Two invocations in the same minute get separate artifacts and cannot delete/
  overwrite each other.
- Success/failure/timeout cleanup preserves unrelated files.
Run destructive/collision tests ONLY in disposable fixtures created for the test;
do not overwrite existing user exports to reproduce W4.

## 7. Preserve Auto/data behavior and scope

| Rule | Default ON/OFF | Bounds | Step | Inclusive gap |
|---|---|---|---|---|
| Fan / CO2 ppm | >=1000 / <=800 | 0..10000 | 50 | ON >= OFF+50 |
| Purifier / unitless VOC | >=150 / <=100 | 1..500 | 5 | ON >= OFF+5 |
| Humidifier / %RH | <=40 / >=50 | 0..100 | 1 | OFF >= ON+5 |

Per-device limits are independent; valid imported integers need not be step
multiples. Clamp only the edited value; invalid gap disables Save without silently
changing its partner. Preset change loads its defaults; unsupported presets are
Manual-only. These are demo settings, not health/appliance-protection standards.
No ppb conversion or new Auto rule.

Missing/invalid/legacy config defaults Manual; only complete valid explicit current
Auto config opts in. Three eligible fresh frames plus10-second hold remain required
after enable, committed limit change, confirmed transition, boot/reconnect or
sensor recovery. Keep wrap-safe ticks/revisions, relevant fault masking, pending
arbitration and per-channel intent. No new decision while offline/invalid/pending/
latched; manual override commits Manual, no timed return. No-op Save resets nothing
and triggers no callback. Latch clears only through explicit Retry or deliberate
committed Manual->Auto. No forced-off guarantee during link loss.

## 8. Deliver evidence, not another unsupported all-pass declaration

Run tools/build_sim.bat, tools/run_regression.bat, tools/export_mcu.bat --verify
and corrected tools/verify_production.bat with cached dependency updates disconnected.
Label approved outside-sandbox reruns versus environment failures honestly.

Put new report, reproducible tests/logs and native800x480 captures in
docs/reviews/2026-09-15-auto-w1-w4-fixes/:
- W1–W4 -> changed files -> named tests/preconditions -> measured outcomes.
- Timeout, late ACK and reconnect with Retry visible on both pages; actual pointer
  re-arm; honest readback and layout preservation. Do not mask controls with toast
  in the inspection captures.
- Per-state/transient/stable heap tables and redraw counts; proof of real TX
  records and true pre-init restore/sensor recovery.
- Artifact collision/ownership tests and actual exported-source build provenance.
- Remaining failures/untested cases explicitly OPEN. Host-only validation is not
  physical MCU/LCD/touch/radio/load acceptance.

Update PLAN, DEV_LOG, relevant docs/indexes and product notes. Preserve old reports
but supersede false current all-complete claims. Do not overwrite CURRENT with an
execution report or rewrite its requirements to legalize an omission.
When delivering a review with remaining issues, follow the owner's automatic
CURRENT prompt rule. No commit/push.


